# Agenthon 2026 Track 4 submission

Containerized bounded-hybrid agent for Agenthon 2026 Track 4.

Runtime contract:

`analyze --task /input/task.json --corpus /input/corpus/ --out /output/answer.json`

The image uses the organizer-provided House model only through the injected restricted runtime endpoint. No credentials or competition data are included in this repository.
